projects:
  - id: pulse
    display_name: Pulse (reconstrução do tempo-resposta-app)
    path: ~/dev/pulse  # caminho por maquina: /Users/jp/dev/pulse (macbook-jpazv), /Users/grupovelas/dev/pulse (mac-grupovelas)
    repo: https://github.com/Grupo-Velas/pulse  # transferido de jpazv/pulse em 2026-07-23
    status: active
    context: work
    agents:
      - claude-code
    notes: >
      Reconstrução full-stack (Next.js+TS+Supabase) do SaaS "Pulse Eco"
      (análise de atendimento via WhatsApp — antes chamado "tempo de
      resposta", projeto antigo em ~/Documents/tempo-resposta-app, permanece
      rodando em produção até o corte da Fase 4). Plano completo em
      docs/plan-reconstrucao.md dentro do próprio repo. Fases 0-3 concluídas.
      Nesta sessão (2026-07-23): endpoint /api/onboarding/provision +
      página /convite — onboarding pós-"compra" do Eco (chamado pelo
      raiox-mvp-html, ver esse projeto abaixo). MVP no ar em produção:
      https://pulse-app-steel-tau.vercel.app (login demo@pulse.local /
      demo12345, modo demo). ⚠️ Deploy do pulse-app travado em "Building"
      por 40+min nesta sessão (3 tentativas) — não resolvido, ver handoff
      memory/handoffs/2026-07-23-pulse-eco-onboarding.md. Próximo: resolver
      o deploy travado, terminar Fase 4 (corte de produção).

  - id: raiox-mvp-html
    display_name: Pulse Lume + Pulse Eco (landing/checkout)
    path: ~/dev/raiox-mvp-html  # movido de ~/Documents/raiox-mvp-html em 2026-07-24 (ver handoff)
    repo: https://github.com/Grupo-Velas/raiox-mvp-html
    status: active
    context: work
    agents:
      - claude-code
    notes: >
      Landing estática (Vercel, sem framework) que vende dois produtos:
      "Pulse Lume" (diagnóstico financeiro, ex-"Raio-X", checkout simulado
      via api/create-stripe-checkout.js/comprar.html) e "Pulse Eco" (análise
      de atendimento WhatsApp — comprar-eco.html + api/pulse-eco-checkout.js,
      que chama /api/onboarding/provision no repo pulse pra criar a conta e
      manda o email de acesso pelo webhook interno "Connect", mesmo canal
      que já envia o resultado do Lume). Sem conta Stripe real ainda — o
      "pagamento" do Eco é simulado de propósito. Primeiro commit git em
      2026-07-23 (nunca tinha sido versionado antes, vivia só no iCloud);
      movido de ~/Documents pra ~/dev em 2026-07-24 depois que
      ~/Documents ficou inacessível (EPERM) no mac-grupovelas — ver handoff
      2026-07-24. No ar: https://raiox-mvp-html.vercel.app (aliases:
      https://pulse-raiox.vercel.app, https://checkpulse.com.br — DNS do
      checkpulse não configurado, sem acesso). ⚠️ Alias curto não está
      promovendo pro deploy mais recente (mesmo sintoma "not ready"/
      autorização visto no pulse-app) — ver handoff 2026-07-24.
      Handoffs: memory/handoffs/2026-07-23-pulse-eco-onboarding.md,
      memory/handoffs/2026-07-24-css-fixes-e-icloud-lockout.md.

  - id: lead-quality-score
    display_name: Score de Qualidade de Atendimento (experimento)
    path: projects/lead-quality-score  # vive DENTRO do hub, nao tem repo proprio
    repo: null  # promover a repo proprio no Grupo-Velas se sair da fase de teste
    status: active
    context: work
    agents:
      - claude-code
    notes: >
      Experimento de analise sobre o Metabase do Grupo Velas — NAO e o Pulse.
      Testa se uma metrica de qualidade de atendimento melhor que a atual
      adiciona poder explicativo a conversao de lead. Somente leitura: nada e
      escrito no BD Grupo Velas (guarda em mb._assert_readonly bloqueia
      qualquer coisa que nao seja SELECT/WITH). Entrega em Excel, em
      projects/lead-quality-score/output/.
      Rodar: python3 -m venv .venv && .venv/bin/pip install -r requirements.txt,
      gravar token do Metabase em .metabase_session (gitignored, NAO commitar),
      .venv/bin/python extract.py (~4min, gera cache/ que nao e versionado),
      depois analyze.py / regressao.py / comparativo.py (~40s cada).
      Resultado principal: a qualidade agrega apenas 0,48% de R2 unico sobre a
      temperatura; volume de conversa domina; %CVS e alvo contaminado (+65% de
      agendamentos nao rastreaveis); 57% da base cega por falta de transcricao
      de midia. Detalhes e proximos passos no handoff
      memory/handoffs/2026-08-05-lead-quality-score-experimento.md.

# Adicione projetos aqui conforme conectar ao hub.
# Exemplo:
#   - id: meu-projeto
#     display_name: Meu Projeto
#     path: /Users/grupovelas/projetos/meu-projeto
#     repo: https://github.com/jpazv/meu-projeto
#     status: active
#     context: personal
#     agents:
#       - claude-code
#     notes: descricao breve do projeto

# Status disponiveis:
#   active   → projeto em andamento
#   paused   → pausado temporariamente
#   archived → concluido ou abandonado
